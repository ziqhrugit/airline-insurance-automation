
{
  "name": "airline-insurance-automation",
  "version": "1.0.0",
  "description": "Automation testing for airline booking with travel insurance",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "typescript": "^5.0.0"
  }
}